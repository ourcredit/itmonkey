<style lang="less">

</style>
<template>
  <view class="container">
  <family @childFn.user="goChat" >
  </family>
  </view>
</template>

<script>
import wepy from 'wepy';
import Family from '@/components/group';
export default class Dash extends wepy.page {
  data = {};
  components = {
    family: Family
  };
  computed = {
    now() {
      return +new Date();
    }
  };
  // 方法
  methods = {
    goChat(item, evt) {
      this.$parent.globalData.user.ChatId = item.id;
      this.$navigate('chat');
    }
  };

  events = {};
  // 加载函数
  async onLoad() {}
}
</script>
